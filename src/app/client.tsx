"use client";

import type { JSX } from "react";
import { usePage } from "@/core/orchestration";

export function HomeClient(): JSX.Element {
  return <main className="relative z-10 min-h-screen">BASE FRAMEWORK</main>;
}
